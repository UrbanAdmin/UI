export const environment = {
  production: true,
  // Placeholder until the Render service is created - update once you know
  // the real service URL (Render shows it as https://<service-name>.onrender.com).
  apiUrl: 'https://urbanadmin-api.onrender.com',
};
