namespace UrbanAdmin.Tenant.Mobile;

public static class AppConfig
{
    // Placeholder - point this at the real deployed Backend before shipping
    // (e.g. via a build-time constant per configuration, or MAUI's
    // appsettings pattern). Never a plaintext credential, so this is safe to
    // commit, unlike the Firebase server key (Constitution Principle I).
    public const string BackendBaseUrl = "https://urbanadmin-backend.example.com";
}
